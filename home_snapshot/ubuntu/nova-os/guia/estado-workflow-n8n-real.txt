WORKFLOW REAL AJUSTADO

Workflow correcto:
- Nombre: Xus Outreach Engine - V1.0
- ID: S5U-InyurlAZCNkpoGJZT
- Archivo fuente: /home/ubuntu/Workflows-n8n/Xus Outreach Engine - V1.0 (1).json
- Estado final actual: activo / producción

Estado operativo real:
- Cron activo: 0 9 * * *
- Timezone del runtime n8n: America/Bogota
- El workflow ya quedó activado en la instancia viva.
- Los nodos vivos y conexiones vivas coinciden con el archivo endurecido del repo.

Qué se cambió de verdad:
- La rama send ya no va directo a Gmail.
- Primero construye una query real contra Gmail Sent usando el destinatario.
- Luego busca coincidencias reales usando la credencial OAuth viva de Gmail en n8n.
- Si encuentra coincidencias, bloquea el envío antes de Gmail Send.
- Si no encuentra duplicado, pasa por Nova Evaluate.
- Solo si Nova aprueba, el flujo llega a Gmail Send.
- El workflow ya no depende de nodos LangChain para clasificar ni personalizar.
- La ruta operativa quedó 100% determinista en n8n con nodos Code, Gmail, Sheets y Nova.
- El branding del correo quedó reorientado a Nova y al asset público canónico servido desde el frontend.

Nodos clave presentes en el workflow vivo:
- 🔎 Build Gmail Sent Query
- 📬 Search Sent Gmail
- 🧮 Analyze Gmail Sent Matches
- ❌ Already Sent In Gmail?
- 🛑 Prepare Gmail Duplicate Skip
- 🛡️ Nova Evaluate Send
- 🔀 Nova Approved?
- 🛑 Prepare Nova Denied Skip

Configuración viva del nodo Nova:
- Credencial: Nova Workflow QA
- Managed Agent ID: agent_6d3a8916ffa32c0d
- Legacy Token ID: 3
- Acción gobernada: send_email

Comportamiento final:
- Ya existe un envío previo al mismo destinatario en Gmail Sent: se bloquea.
- Nova no aprueba: se bloquea.
- Sin duplicado y Nova aprueba: se envía.
- Si 100 contactos llegan a la rama de envío, la verificación contra Gmail Sent se ejecuta 100 veces, una por destinatario.

Validaciones hechas:
- Postgres confirma que el workflow vivo contiene los nodos de Gmail duplicate check.
- Postgres confirma que el nodo Nova vivo usa la credencial correcta y el legacy token correcto.
- Gmail real devolvió coincidencia para búsquedas en Sent con la credencial viva.
- n8n volvió sano tras reinicio.
- El paquete del nodo Nova quedó actualizado con icono light/dark y fallback /validate.
- Postgres confirma que el workflow vivo quedó activo.
- Postgres confirma que el workflow vivo ya no contiene nodos langchain.
- Se verificó que el límite de Gmail Search quedó en 1 para resolver el duplicate check con el mínimo costo por ejecución.

Artefactos útiles:
- Backup de nodos vivos antes del ajuste: /home/ubuntu/nova-os/guia/xus-outreach-engine.live-nodes.backup.json
- Backup de conexiones vivas: /home/ubuntu/nova-os/guia/xus-outreach-engine.live-connections.backup.json
- Workflow preparado en repo: /home/ubuntu/nova-os/guia/xus-outreach-engine.nova-ready.json
- Export vivo verificado tras el patch final: /home/ubuntu/nova-os/guia/xus-outreach-live-verified.json

Artefacto que no debes usar como fuente final:
- /home/ubuntu/nova-os/guia/xus-outreach-after-import.json
- Ese archivo quedó viejo y puede mostrar una mezcla anterior de la definición.

Nota crítica:
- La deduplicación histórica real contra Gmail hoy está resuelta dentro de este workflow de n8n.
- No es todavía una capacidad genérica del backend de Nova para cualquier superficie.
- El criterio actual de bloqueo histórico en este flujo es por destinatario consultando `in:sent to:<recipient>`.
